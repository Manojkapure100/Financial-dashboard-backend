# Financial-dashboard-backend

To install : pip install "fastapi[standard]"

To start: uvicorn server:app --reload

=== We have ===

- NSELIB = 
- India API = 
    - Get the detail of stock but we only can hit 500 API in month
    - We fetch and store in databse and use it always from databse
    - how can i get real time price of the stock.
- Angle One API =
    - Get the current price